export const isFetching = ({fetching}) => fetching;

export const isFetched = ({fetched}) => fetched;
export const isChanged = ({changed}) => changed;
export const isUserListEmpty = ({users}) => users.length === 0;
